config.stopBubbling = true

# Marks Lombok-generated methods (getters, setters, equals, hashCode, toString,
# builders) with @lombok.Generated so JaCoCo leaves them out of the coverage
# report. Without this, generated code we never call by hand blocks 100%.
lombok.addLombokGeneratedAnnotation = true
